# 첨부 목록

## 다른 GPT에 한 번에 전달할 파일

1. 이 폴더 전체
2. 루트 `README.md`
3. 루트 `HANDOFF.md`
4. 루트 `prd.md`
5. 루트 `visual-references.md`
6. `supabase/migrations/`
7. 필요 시 현재 소스 전체 저장소 링크

## 권장 전달 문구

이 ZIP은 맞춤장사 OS의 현재 구현과 목표 설계를 함께 담은 인수인계 문서팩이다. 먼저 `README.md`, `HANDOFF.md`, `docs/handoff-2026-07-19/05_IMPLEMENTATION_STATUS.md`를 읽고 화면이 있다는 이유로 기능을 완료로 판단하지 마라. `store_id` 중심 DB 원장을 먼저 완성하고 `localStorage` 기능을 Supabase로 이전한 뒤 페이지별 로직을 연결하라.

