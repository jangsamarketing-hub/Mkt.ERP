# 기존 프로그램 통합 메모

## 1. 투두리스트 웹앱

첨부 파일: `투두리스트 웹앱.zip`

확인 결과:

- `TodoCheck-V1-final` 프로젝트는 원본 개발 소스보다 `dist` 빌드 결과 중심으로 들어 있다.
- `package.json`은 `npx serve dist` 실행 구조다.
- ERP에 그대로 합치기보다는 기능 개념을 새 ERP의 주간 업무/추가 업무 구조로 재구현하는 편이 적합하다.

ERP 반영 방향:

- 정기 업무 체크리스트와 별도로 `추가 업무 / 약속 히스토리`를 둔다.
- 추가 업무는 공용 업무판에 등록하되, 등록된 매장과 매칭한다.
- 필드 예시:
  - 매장 ID
  - 업무 제목
  - 업무 출처: 통화, 카카오톡, 내부 메모, 사장님 요청
  - 담당자
  - 마감일
  - 상태: 대기중, 진행중, 완료, 보류
  - 업무 메모
  - 첨부 파일
  - 사장님 보고서 노출 여부
- 이후 확장:
  - 통화 녹음 파일 업로드
  - AI가 약속/요청사항 추출
  - 추출된 내용을 추가 업무로 자동 생성

## 2. 네이버 플레이스 태그 자동화

첨부 파일: `naver_place_tag_automation.zip`

확인 결과:

- Python + Playwright 기반 자동화 도구다.
- 로그인 자동화, 캡차 우회, 광고 예산 변경, 광고 ON/OFF, 광고 삭제는 하지 않는 구조다.
- 사용자가 이미 로그인한 네이버 광고센터 화면에서 태그 입력 모달을 열어둔 상태로 태그 50개 입력을 보조한다.
- CSV 입력 구조는 `campaign_name`, `ad_group_name`, `tag_set_id`, `tag_order`, `tag` 기반이다.
- 태그 검증 규칙:
  - 세트당 50개
  - 태그 길이 1~10자
  - 중복/빈값/특수문자 검증
  - 실패 로그와 스크린샷 저장

ERP 반영 방향:

- 키워드 조합기에서 생성한 태그용 50개 세트를 CSV로 다운로드한다.
- 다운로드 CSV는 태그 자동화 프로그램이 읽을 수 있는 형식으로 확장한다.
- MVP에서는 수동 다운로드 + 수동 실행으로 시작한다.
- 이후 ERP에서 자동화 작업 요청을 만들고, 별도 워커 또는 로컬 실행 도구가 Playwright 자동화를 수행한다.

주의:

- 이 기능은 네이버 정책과 화면 구조 변경에 영향을 받을 수 있다.
- MVP에서는 `준비중` 버튼으로 위치만 잡고, 저장까지 자동화는 충분히 테스트한 뒤 단계적으로 붙인다.
